import { Container, TextLabel } from "./style";

export default function Header() {
    return (
        <Container>
            <TextLabel>
                Repo list with RN✨
            </TextLabel>
        </Container>
    );
}